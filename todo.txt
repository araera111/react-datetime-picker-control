ここにTODOを置いて管理します
1. ←、→で隣のカーソルにうつる

まずは日付から、各inputを表示する

年について考える。
ここでは、右キーを押すと次の入力欄へ移動する。左キーを押すと一番最後のコントロールに戻る。
上、下で数字を変更できる
数字を入力することもできる
数字以外の入力は破棄する
数字をどんどん入力してスロットに入れていくことができる
スロットは4つまで保持できる。
ただ、2文字のときも対応している
2文字のときにフォーカスが外れると、たとえば22であれば2022に自動補正される。
4文字のときにフォーカスが外れて、invalidな値でなければそれがそのまま表示。invalidな値であればもともとの数字に戻る。

つまりon blur時？　そもそも


元の値が05で、3を押すと3月になる。そのまま。
上下で数値が変わる。
3->1で1維持
3->1->1で11になる。
現在の数字が1のときだけ、1と2の入力が認められる。
1以外の数字のときは、その数字で確定する。
1が入ったときは、1月になるが
前の数字が1だったときだけ、次の入力に1と2が認められる。
1->3などは3になる